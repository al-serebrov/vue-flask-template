version: '3.3'
services:
  backend:
    build:
        context: backend
        dockerfile: Dockerfile
    restart: always
    container_name: backend
    user: "${UID}:${GID}"
    command: flask run
    volumes:
      - ./backend:/backend
    ports:
      - "5000:5000"
    env_file:
        - ./backend/config.env
    environment:
      FLASK_APP: /backend/app.py
      FLASK_RUN_HOST: 0.0.0.0
      FLASK_CONFIG: local
      FLASK_ENV: development
      SQLALCHEMY_DATABASE_URI: 'postgresql://tester:complicatedpass@postgresql:5432/autoria'
      SQLALCHEMY_TEST_DATABASE_URI: 'postgresql://tester:complicatedpass@postgresql:5432/autoria'

  postgresql:
    image: postgres:11.2
    user: "${UID}:${GID}"
    environment:
      POSTGRES_USER: tester
      POSTGRES_PASSWORD: complicatedpass
    ports:
      - "5432:5432"
    volumes:
      - ./docker_data/postgresql:/var/lib/postgresql/data

  frontend:
    container_name: frontend
    build:
      context: frontend
      dockerfile: Dockerfile
    volumes:
      - './frontend:/frontend'
      - '/frontend/node_modules'
    ports:
      - '8081:8080'
